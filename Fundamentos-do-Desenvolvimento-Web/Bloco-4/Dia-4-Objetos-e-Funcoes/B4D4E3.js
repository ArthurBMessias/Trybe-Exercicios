// qual é o indice de maior valor?

let bigestValue = '';
let nElementos =[2, 3, 6, 7, 10, 1];

function whoIsTheBigest(array) {
    for ( let i = 0; i <= array.length; i += 1);
    if (array.length[i] > bigestValue) {
    bigestValue = array[i]
    }
}
console.log (whoIsTheBigest(nElementos[bigestValue]))